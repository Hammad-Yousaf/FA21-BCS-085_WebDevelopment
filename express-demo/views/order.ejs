<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1>Order Confirmation</h1>
    <p>Thank you, <%= order.user.username %>, for purchasing <%= order.car.make %> <%= order.car.model %>.</p>
    <p>Order Status: <%= order.status %></p>
    <p>Price: <%= order.car.price %> <%= preferredPriceUnit === 'PKR' ? 'PKR' : 'USD' %></p>
    <a href="/cars" class="btn btn-primary">Back to Car Listings</a>
  </div>
</body>
</html>
